
def merge_the_tools(x,k):
    s=x.strip()
    l=len(s)//k  #//for round answer
    for i in range(l):
        t=''
        for c in s[i*k:i*k+k]:
            if c not in t: t+=c
        print(t)
    


if __name__ == '__main__':
    string, k = input(), int(input())
    merge_the_tools(string, k)
